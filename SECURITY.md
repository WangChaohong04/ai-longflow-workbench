# 安全与部署边界

LongFlow 当前面向本地、单机研究与演示，不是开箱即用的公网多租户服务。

## 默认安全边界

- 默认监听 `127.0.0.1:8765`。
- 未配置认证 token 时，后端使用本地单用户管理员模式；这不是登录保护。不要直接监听公网或暴露端口。
- 配置 `LONGFLOW_AUTH_TOKEN` 可开启 Bearer token 认证。公开部署仍需自行审查 TLS、反向代理、访问控制、限流和运维措施。
- 插件是可信本地代码，没有不可信代码沙箱。不要加载来源不明的插件。
- 采购和通知等样例工具不代表已接入真实外部业务系统；审批通过也不代表任何任意工具都安全。
- 外部搜索与模型可能接收用户任务或资料，请先确认数据授权及服务商政策。

## 凭证与运行数据

不要提交 `.env`、认证 token、模型 API key、运行数据库、日志、用户上传资料或本地会话文件。仅发布无凭证的示例配置。

`.gitignore` 不能撤回已经提交的秘密。如果发生泄漏，应先撤销/轮换凭证，再处理仓库历史；仅删除文件不足以消除风险。

## 问题报告

请勿在公开 Issue 中粘贴有效密钥、私人文档或完整运行数据库。非敏感问题可以附最小复现、软件版本及脱敏日志。若仓库开启 GitHub 私密漏洞报告，请通过该入口报告敏感安全问题。
